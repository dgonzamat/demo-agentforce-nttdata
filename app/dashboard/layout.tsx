import type { ReactNode } from "react"

export const metadata = {
  title: "Agentforce Action Tracker · NTT DATA",
  description: "Panel de control para monitorear, gestionar y optimizar las acciones de tus agentes Agentforce.",
}

export default function DashboardLayout({ children }: { children: ReactNode }) {
  return children
}
